# Win7 虚拟机手动验证清单

> 配套 `win7-verify/` 打包产物 `dist/linz-0.1.0-win7-setup.exe`。
> 本机（Win11）已用相同 Electron 22 / Node 16.17.1 运行时验证过主进程启动、流式链路、pi engine 降级，本清单覆盖只能在真实 GUI + Win7 上做的交互验证。
> 验证日期：____  验证人：____

## 0. 前置准备
- [ ] 虚拟机能访问 `https://api.deepseek.com`（联网），或已配好 Ollama 离线模式
- [ ] 已有 DeepSeek API key

## 1. 配置 API Key
1. 打开应用 → 设置（侧边栏齿轮）
2. 填入 DeepSeek API key，保存
- **预期**：设置保存成功，侧边栏底部模型状态指示器变绿（云端）
- **失败定位**：若保存后仍灰色"离线"，说明配置未写入 electron-store，查 `%APPDATA%/linz/config.json` 是否生成

## 2. 普通对话（最关键，验证流式链路）⭐
1. 新建对话，发送：`你好，请用一句话介绍自己`
- **预期**：逐字流式输出，不是长时间卡住后一次性出现
- **失败定位**：
  - 完全无响应 → 主进程 LLM 调用挂了。从 `cmd` 运行 `linz.exe` 抓 stdout 看报错
  - 报 `fetch is not defined` → node-fetch 回退失效，是回归，需反馈
  - 一直转圈 → 网络 / API key 问题

## 3. 对话终止按钮 ⭐
1. 发送一个长问题（如"写一篇 500 字关于飞机机翼设计的文章"）
2. 输出到一半点**终止/停止**按钮
- **预期**：输出立即停止，气泡不再追加内容
- **备注**：`待办.md` 记了"对话终止按钮不管用"，此步验证该问题是否仍存在

## 4. Agent 调度（多 Agent 并发）
1. 发送跨领域问题：`评估 NACA0012 翼型在低速下的气动特性，并简要说明结构强度关注点`
- **预期**：先出协调器分析，然后 aero + structural 两个 Agent 各自气泡**交错**流式输出
- **失败定位**：只有一个 Agent 输出 → 调度关键词没匹配；全卡住 → 并发限制器 `createLimit` 问题

## 5. pi engine 回退验证 ⭐（对应本机实测风险）
1. 普通对话 Agent 默认 engine=pi，发消息
- **预期**：**正常出结果**（后台实际走了 DeepSeek 回退），不弹错、不卡死
- **验证方式**：结果能出来即通过。Win7 上 pi SDK 必加载失败（Node 16 无法解析其正则 flag），能出字就证明回退链路 OK

## 6. 消息持久化
1. 发几条消息后，**完全退出**应用（托盘右键 → 退出，不是关窗口）
2. 重新打开，进入刚才的对话
- **预期**：历史消息完整显示
- **失败定位**：消息丢失 → SQLite `saveDatabase()` 失败；内容残留 `<<<AGENT_MSG>>>` 标记 → 持久化 strip 失效（已知问题边界）

## 7. 工作区切换
1. 侧边栏顶部工作区切换器 → 新建一个工作区 → 切换
- **预期**：页面自动刷新，新工作区对话列表为空；切回"默认项目"，原对话还在
- **失败定位**：切换后白屏 → `switchDatabase()` 重初始化失败

## 8. 知识库（可选，验证 embedding 回退）
1. 知识库页上传一个 `.txt` 或 `.md` 文件
2. 回到对话问相关问题
- **预期**：能检索到并注入 `[知识库参考]`。embedding 模型在 Win7 大概率加载不了，应**自动回退 BM25** 仍能搜到
- **失败定位**：上传即报错 → 文件解析器问题；搜不到 → 混合检索回退失效

---

**优先级**：2、3、5 是核心（直接对应本机实测的三个风险点），先跑这三个。
任何一步失败，记录现象 + 从 `cmd` 启动 `linz.exe` 抓到的报错，反馈处理。

## 本机已验证项（无需在 Win7 重复）
- [x] 主进程启动：DB 种子、CATIA(55 工具)/Abaqus(8 工具) MCP 连接全 OK
- [x] 流式链路：`@langchain/openai` → `openai@4.104` 走 `node-fetch@2.7.0`(CJS)，不依赖 Node 18 全局 fetch
- [x] pi engine：`await import()` 在 Node 16 抛 `Invalid regular expression flags`，被 `ensurePi()` 捕获后回退 DeepSeek，不崩
